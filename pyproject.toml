[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fleet_replacement"]

[project]
name = "fleet_replacement"
version = "0.0.1"
dependencies = ["gymnasium", "pygame>=2.1.3", "pre-commit", "pyyaml>=6.0"]
